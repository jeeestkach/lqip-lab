<script setup lang="ts">
/**
 * Обычная оболочка страницы: навигация плюс содержимое.
 *
 * Вынесена из `app.vue` в лейаут, чтобы демо-страницы могли от неё отказаться
 * через `definePageMeta({ layout: false })`. Открытые сами по себе, они должны
 * выглядеть настоящей витриной, а не разделом стенда: шапка там только мешает
 * и вносит в замер лишние байты.
 */
</script>

<template>
  <div>
    <nav class="topbar">
      <NuxtLink to="/" class="brand">Демка загрузки изображений</NuxtLink>
      <NuxtLink to="/compare">Сравнение</NuxtLink>
      <NuxtLink to="/">Каталог</NuxtLink>
      <NuxtLink to="/upload">Загрузка</NuxtLink>
      <NuxtLink to="/why">Зачем это нужно</NuxtLink>

      <!--
        Обычные ссылки, а не NuxtLink, и в новой вкладке.

        Обе стратегии надо открыть по отдельности и поставить окна рядом:
        внутри одного документа настоящую загрузку не воспроизвести, а переход
        средствами маршрутизатора не перезагружает документ — то есть показывает
        что угодно, кроме того, ради чего страницы написаны.

        Прокрутка у них общая: соседние вкладки договариваются между собой,
        поэтому листать можно любую. `noopener` этому не мешает — канал
        связывает вкладки по общему происхождению, а не по родству окон.
      -->
      <span class="topbar-pair">
        <span class="topbar-label">открыть рядом:</span>
        <a href="/demo/ssr" target="_blank" rel="noopener">серверный рендер</a>
        <a href="/demo/csr" target="_blank" rel="noopener">клиентский</a>
      </span>
    </nav>
    <slot />
  </div>
</template>
