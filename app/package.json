{
  "name": "@ipc/demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SSR-демка трёхступенчатой загрузки изображений: плейсхолдер в HTML → карточное изображение → полный размер",
  "scripts": {
    "dev": "nuxt dev --port 3010",
    "build": "nuxt build",
    "preview": "nuxt preview --port 3010",
    "seed": "node scripts/seed.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "h3-compression": "^1.1.0",
    "nuxt": "^4.2.0",
    "sharp": "^0.34.2",
    "vue": "^3.5.13"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp"
    ]
  }
}
